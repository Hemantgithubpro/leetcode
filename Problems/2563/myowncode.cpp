#include <bits/stdc++.h>
using namespace std;

bool iffair(vector<int> nums, int l, int r){
	// we check l,r in nums

}

long long countFairPairs(vector<int>& nums, int lower, int upper) {
	// lower <= nums[i] + nums[j] <= upper
	int i=0;
	long long count=0;
	while(i<nums.size()){
		count++;
		i++;
	}
}

int main(){
    
}