#include <bits/stdc++.h>
using namespace std;

void maxDifference(string s) {
        int arr[26];
        for(int i=0; i<s.length();i++){
            cout<<s[i]-97;
        }
    }

int main(){
    string s;
    cin>>s;
    maxDifference(s);
    return 0;
}