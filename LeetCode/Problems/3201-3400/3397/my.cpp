#include <bits/stdc++.h>
using namespace std;
int maxDistinctElements(vector<int> &nums, int k)
{
    unordered_map<int, int> um;
    for (int n : nums)
        um[n]++;

    for(int n:nums){
        if(um[n]>1) 
    }
}

int main()
{
}