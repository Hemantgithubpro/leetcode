#include <bits/stdc++.h>
using namespace std;

// left it here, not able to do
// create a vector for finding all the possible routes and its value
vector<int> n(vector<int> nums, int k){
	vector<int> a;
	for(int i=0; i<nums.size(); i++){
		int count=0;
		for(int j=0; j<k; j++){

		}
	}
}

// then find the minimum among these vector


int minCapability(vector<int>& nums, int k) {
	
}

// nums=[2,7,9,3,1] , k=2


// nums=[2,3,5,9] , k=2


int main(){
	vector<int> nums={2,7,9,3,1};
	cout<<minCapability(nums,3);
}