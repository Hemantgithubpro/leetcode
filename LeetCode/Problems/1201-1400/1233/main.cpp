// using Set
#include <bits/stdc++.h>
using namespace std;

vector<string> removeSubfolders(vector<string> &folder)
{
    sort(folder.begin(), folder.end());
    unordered_set<string> us;
    for (int i = 0; i < folder.size(); i++)
    {
        int n=folder[i].size()-1;
        while(n>1){
            
        }
    }
}

int main()
{
}