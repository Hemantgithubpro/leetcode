#include <bits/stdc++.h>
using namespace std;

int main(){
    int a=66;
    cout<<char(a)<<endl;
    cout<<to_string(a)<<endl; // this is not correct, it will give the ascii value of a as string, not the character
}