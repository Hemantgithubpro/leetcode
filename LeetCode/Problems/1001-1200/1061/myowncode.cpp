#include <bits/stdc++.h>
using namespace std;

string smallestEquivalentString(string s1, string s2, string baseStr)
{
    // we've to map/put it in vector/use linkedlist , just so that we can map it out.
    vector<char> v 
    for (int i = 0; s2.size(); i++)
    {
    }
}

int main()
{
    string s1 = "parker";
    string s2 = "morris";
    string baseStr = "parser";

    cout << smallestEquivalentString(s1, s2, baseStr);
}