#include <bits/stdc++.h>
using namespace std;

vector<string> readBinaryWatch(int turnedOn) {
       
}

int main(){ // it uses backtracking, i'll skip for now.
    // permutation combination, min lights in both=0
    // hours: maximum limit=12, max lights=1, 
    // minutes: maximum limit=59, max lights=5
    // so 0<=turnedOn<=6
    // so for combination: 
    

}