#include <bits/stdc++.h>
using namespace std;

// i could've done this, if i had time. will do it later
void solve(int x, int target, int k){
    // x=4, 6, 3
    // lcm of 4,6 = 12
    // lcm/x <=k
    // x*3 = 12
    // 12/2

    // 780 23 42
    // hcf of these two
    // 

    // 1 982800 13


    int a;
    // x=x*a; or x=x/a

    cout<<endl;
}

int main() {
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // Your code goes here
    int t;
    cin >> t;
    while (t--) {
        // input
        int x;
        cin>>x;
        int y;
        cin>>y;
        int k;
        cin>>k;
        solve(x,y,k);
    }

    return 0;
}
